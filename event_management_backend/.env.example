# Backend configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# MongoDB configuration (provided by database container)
MONGODB_URL=mongodb://username:password@host:port
MONGODB_DB=eventmaster

# Authentication
JWT_SECRET=replace_with_strong_secret
JWT_EXPIRES_IN=7d
